import { ComingSoon } from "@/components/admin/ComingSoon";

export const dynamic = "force-dynamic";

export default function CTALinksAdminPage() {
  return (
    <ComingSoon
      title="CTA Links"
      phase="Phase 5"
      description="CRUD untuk link-in-bio style CTA cards (Chat WhatsApp, Katalog, Promo, dst). Edit title, description, href, accent color, icon, dan urutan."
    />
  );
}
