import { ComingSoon } from "@/components/admin/ComingSoon";

export const dynamic = "force-dynamic";

export default function StatsAdminPage() {
  return (
    <ComingSoon
      title="Stats Grid"
      phase="Phase 5"
      description="CRUD untuk 4 stats card di landing page (350K+ Order Selesai, 9K+ Klien Puas, dst). Bisa edit angka, label, icon, dan urutan."
    />
  );
}
