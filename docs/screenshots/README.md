# `docs/screenshots/` — Tangkapan layar untuk README

Folder ini menampung tangkapan layar halaman project yang ditampilkan di README utama.

## Yang dibutuhkan (prioritas)

| File | Halaman | Catatan |
| --- | --- | --- |
| `homepage.png` | `/` | Hero + katalog, viewport desktop 1440×900 |
| `katalog.png` | `/katalog` | Grid desain + filter kategori |
| `category-landing.png` | `/jersey-futsal` | Contoh landing page kategori |
| `tracking.png` | `/track` atau `/status` | Timeline produksi customer |
| `admin-dashboard.png` | `/pesanan/orders` | **Wajib redact nama & nomor HP customer sebelum di-commit** |

## Aturan

- Semua screenshot harus dari halaman **production** (`www.tntsportapparel.id`).
- Screenshot dashboard yang menampilkan data customer **wajib di-redact** (blur/tutup nama, nomor HP, foto) sebelum masuk repo — repo ini public.
- Format PNG, ukuran viewport konsisten (disarankan 1440×900).
- Jangan taruh aset di luar folder ini; `_archive/screenshots/` hanya untuk referensi historis.
